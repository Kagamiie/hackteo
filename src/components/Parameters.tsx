import TextLine from "./TextLine";

function Parameters() {
    return (
        <div style={{padding:'var(--ma-m)', boxSizing:'border-box'}}>
            <TextLine placeholder="Enter valid address"></TextLine>
        </div>
    )
}

export default Parameters;